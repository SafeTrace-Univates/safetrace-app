<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões necessárias para o app -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeTrace"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        
        <!-- LoginActivity - Tela de login (tela inicial) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity - Tela principal com botões de emergência -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        
        <!-- CadastroActivity - Tela de cadastro de usuário -->
        <activity
            android:name=".CadastroActivity"
            android:exported="false" />
        
        <!-- CadastroContatosActivity - Tela de cadastro de contatos -->
        <activity
            android:name=".CadastroContatosActivity"
            android:exported="false" />
        
        <!-- HistoricoActivity - Tela de histórico de chamadas -->
        <activity
            android:name=".HistoricoActivity"
            android:exported="false" />
        
        <!-- ScanQRActivity - Tela para escanear QR Code -->
        <activity
            android:name=".ScanQRActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        
        <!-- TrajetoActivity - Tela para visualizar trajeto no mapa -->
        <activity
            android:name=".TrajetoActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- PerfilActivity - Tela para editar dados do usuário -->
        <activity
            android:name=".PerfilActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        
        <!-- EmergenciaForegroundService - Serviço em foreground para gravação de áudio e localização -->
        <service
            android:name=".service.EmergenciaForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone|location" />

        <!-- App Widget Provider -->
        <receiver
            android:name=".widget.EmergencyWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.safetrace.ACTION_WIDGET_EMERGENCY" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/emergency_widget_info" />
        </receiver>
        
        <!-- NetworkChangeReceiver - Detecta mudanças na conectividade de rede -->
        <!-- Para Android 7.0+ (API 24+), o BroadcastReceiver não funciona mais para CONNECTIVITY_ACTION -->
        <!-- O processamento da fila será feito quando o app iniciar ou voltar ao foreground -->
        <receiver
            android:name=".receiver.NetworkChangeReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_ACTION" />
            </intent-filter>
        </receiver>

        <!-- FileProvider para compartilhar arquivos (áudios) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDS9wMp_SewVIiH9_cTw46hg1Hi4BsiP1A" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>